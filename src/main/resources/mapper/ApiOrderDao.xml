<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.digitalchina.sport.resource.api.dao.ApiOrderDao" >
	<resultMap id="BaseResultMap" type="com.digitalchina.sport.resource.api.model.ApiOrder">
		<id column="id" property="id" jdbcType="INTEGER"/>
		<result column="desc" property="desc" jdbcType="VARCHAR"/>
		<result column="created_time" property="createdTime" jdbcType="TIMESTAMP"/>
	</resultMap>
	<sql id="Base_Column_List">
		`id`, `desc`, `created_time`
	</sql>
	<sql id="selectCondition">
		<if test="id != null and id != ''">
			AND o.`id` = #{id}
		</if>
		<if test="desc != null and desc != ''">
			AND o.`desc` = #{desc}
		</if>
	</sql>

	<select id="getOrderInfoByPK" resultMap="BaseResultMap" parameterType="java.util.HashMap">
		select
		<include refid="Base_Column_List"/>
		from order_info o
		<where>
			<include refid="selectCondition" />
		</where>
	</select>
</mapper>